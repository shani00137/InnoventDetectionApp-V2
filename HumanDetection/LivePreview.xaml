<UserControl x:Class="HumanDetection.LivePreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HumanDetection" 
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border CornerRadius="3" Background="Black" BorderThickness="1" BorderBrush="Indigo">
        <Grid Background="Black" Margin="5,5,5,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Foreground="White" FontSize="20" Text="Live Camera Preview" />
            </StackPanel>

            <!-- Main content: controls + results -->
            <Grid Grid.Row="1" Margin="10,10,10,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Camera & AI Model Controls -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <!-- for Submit -->
                    </Grid.ColumnDefinitions>

                    <!-- Camera Combo -->
                    <ComboBox x:Name="CameraComboBox" Grid.Column="0" Margin="5" HorizontalAlignment="Stretch">
                    </ComboBox>

                    <!-- AI Model -->
                    <ComboBox Grid.Column="1" Margin="5" HorizontalAlignment="Stretch">
                        <ComboBoxItem>Box Detection</ComboBoxItem>
                        <ComboBoxItem>OCR Text</ComboBoxItem>
                        <ComboBoxItem>Human Detection</ComboBoxItem>
                    </ComboBox>

                    <!-- Submit Button -->
                    <Button
        Grid.Column="2"
        Content="Submit"
        Width="120"
        Height="30"
        Margin="5"
        Background="#193D23"
        Foreground="#34C759"
        BorderThickness="0"
        HorizontalAlignment="Stretch"
        Click="SubmitButton_Click" />
                </Grid>


                <!-- Results list -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <!-- your ItemsControl stays same -->
                    <!-- ... -->
                </ScrollViewer>
            </Grid>

            <!-- Bottom buttons -->
            <Grid Grid.Row="2">
                <StackPanel Margin="5,5,7,5"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right">
                
                    <Button
                        Click="CloseButton_Click"
                        Width="100"
                        Content="Close"
                        Foreground="White"
                        VerticalAlignment="Center"
                        HorizontalContentAlignment="Center"
                        Background="Gray"
                        Padding="0"
                        BorderThickness="0" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
